# Ephemeral output from scripts/notify_new_calls.py (issue body for a single run)
fathom_new_calls.md

.DS_Store

# Local Claude Code project config (per-machine, not shared)
.claude/

# Large source proposal doc — kept local, not committed / not deployed via Pages
PROP 01 VARÉ.pdf
